# TARS Lite — the portable core (Mac/Linux)
numpy
sounddevice
vosk
faster-whisper
kokoro-onnx
requests
psutil
python-dotenv
Pillow
ddgs
edge-tts
pyttsx3
truststore
soundfile

# the self-teaching big brain. Installed by default because setup ASKS for
# a Claude token, and "not installed even after putting in the token" is a
# broken promise. It also needs the `claude` command itself — setup_mac.sh
# installs that via npm and says so if it can't.
claude-agent-sdk

# optional extras — uncomment if wanted
# python-telegram-bot     # not needed: the phone bridge uses plain requests
