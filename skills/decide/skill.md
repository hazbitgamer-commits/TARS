# decide
Quick random decision helper for when the owner is unsure.
**Say:** "flip a coin" / "roll a die" / "I'm not too sure" / "pick one: pizza or tacos"
**Args:** `mode` — coin, dice, yesno, or choice. `options` — list of choices (only for choice mode).
