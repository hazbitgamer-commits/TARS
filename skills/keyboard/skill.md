# keyboard
Presses editing keys in the focused window (select all, delete, copy, paste, undo, enter, tabs...).
**Say:** "select all and delete" / "press enter" / "undo that" / "new tab"
**Args:** `actions` — comma-separated sequence.
