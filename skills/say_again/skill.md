# say_again
Reads back TARS own last spoken line (from todays log), optionally slower or spelled out. Skips previous repeats so it cannot echo itself.
**Say:** "say that again" / "repeat that slower" / "spell that"
**Args:** `style` - again/slower/spell.
