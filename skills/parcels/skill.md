# parcels
Finds shipping/order mail in Gmail (subject + courier sender query, newer_than:Nd) and has the background model summarise what is on the way vs delivered. Read-only.
**Say:** "wheres my package" / "what have I got coming" / "any deliveries today"
**Args:** `days` (default 30).
