# calendar
Google Calendar: agenda for today/tomorrow/week, add events (day+time parsing incl. weekday names).
**Say:** "what's on my calendar today" / "add dentist to my calendar friday at 3 pm"
**Args:** `action` — agenda/add; `when`; `time`; `title`.
