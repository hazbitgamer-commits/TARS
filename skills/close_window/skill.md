# close_window
Closes one window by its title (folder windows, browser windows). Won't touch TARS's own window.
**Say:** "close the pictures window" / "close that folder"
**Args:** `title` — part of the window title.
