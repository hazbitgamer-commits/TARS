# snooze
Re-arms the reminder that most recently fired (timers_watch writes last_fired.json when one goes off). Default 9 minutes, refuses if the last one was over an hour ago.
**Say:** "snooze" / "snooze ten minutes" / "five more minutes"
**Args:** `minutes`.
