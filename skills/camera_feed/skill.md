# camera_feed
Live MJPEG webcam view at localhost:8765/camera (served by dashboard.py, ~15fps, local only, released when tab closes). Word-gated: only fires when the owner says camera/webcam.
**Say:** "show my camera feed" / "open the camera"
**Args:** none.
