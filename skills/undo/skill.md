# undo
Reverses the last change TARS made (volume, brightness, settings files, deleted files from the Recycle Bin). One step deep by design. Snapshots are taken by brain.py before undoable skills run — see undo.py KINDS.
**Say:** "undo that" / "put that back"
**Args:** none.
