# delete_files
Deletes a folder's contents — ALWAYS asks for a spoken "yes" first, ALWAYS via Recycle Bin (recoverable). Hard-block list compliant.
**Say:** "delete all screenshots in the TARS folder" → TARS asks → "yes"
**Args:** `target`; `confirmed` (set by the brain only).
