# email
Gmail via Google API: unread check, inbox summary (7b), draft creation. NEVER sends — drafts wait in Gmail for Jacob (hard-block). Needs google_credentials.json + one-time browser approval.
**Say:** "any new emails?" / "summarize my inbox" / "draft an email to mum about sunday dinner"
**Args:** `action` — unread/summarize/draft; `to`; `about`.
